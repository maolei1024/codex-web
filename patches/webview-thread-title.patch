--- a/webview/assets/local-conversation-thread-imfIBhy0.js
+++ b/webview/assets/local-conversation-thread-imfIBhy0.js
@@ -233,6 +233,7 @@
   X5 as Nn,
   X6 as Pn,
   XM as Fn,
+  XN as codexHostedWindowTitleAtom,
   XP as In,
   XS as Ln,
   XY as Rn,
@@ -16012,6 +16013,7 @@
     w = W(zr, e),
     T = W(Ti, e),
     E = W(Ki, e) === `projectless`,
+    codexHostedWindowTitle = W(codexHostedWindowTitleAtom, e),
     D = W(oo, e),
     O = W(Fi, e),
     {
@@ -16045,6 +16047,10 @@
   (0, sC.useEffect)(() => {
     re?.id == null || re.readAt != null || ne(re.id);
   }, [re?.id, re?.readAt, ne]);
+  (0, sC.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let oe = Xt(),
     { agentMode: se, shouldSendPermissionOverrides: ce } = dc({
       conversationId: e,
