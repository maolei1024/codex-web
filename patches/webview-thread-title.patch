--- a/webview/assets/local-conversation-thread-CRryh-25.js
+++ b/webview/assets/local-conversation-thread-CRryh-25.js
@@ -155,6 +155,7 @@
   QY as Dt,
   Qb as Ot,
   Qg as kt,
+  Qw as codexHostedWindowTitleAtom,
   RR as At,
   RV as jt,
   Rq as Mt,
@@ -17068,6 +17069,7 @@
     S = K(er, e),
     T = K(Kt, e),
     E = K(Bn, e) === `projectless`,
+    codexHostedWindowTitle = K(codexHostedWindowTitleAtom, e),
     D = K(C, e),
     O = K(In, e),
     {
@@ -17093,6 +17095,10 @@
   (0, GS.useEffect)(() => {
     U?.id == null || U.readAt != null || ne(U.id);
   }, [U?.id, U?.readAt, ne]);
+  (0, GS.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let ae = $e(),
     { agentMode: oe, shouldSendPermissionOverrides: se } = xt({
       conversationId: e,
